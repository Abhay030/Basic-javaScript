const name = "Abhay"  //values of the const cannot be changed after it is once initialised 
var accountId = "2022.csai-----"  
let pass = 12234
city  = "kanpur"

var accountId = "abhistar@gmail.com"  //updating the var after the first declaration

// Prefer not to use the var type declarations because it have block scope and functional scope issues 

console.log("name")
console.table([name ,accountId , pass , city]);
